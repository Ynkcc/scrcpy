diff --git a/server/src/main/java/com/genymobile/scrcpy/daemon/DaemonArgs.java b/server/src/main/java/com/genymobile/scrcpy/daemon/DaemonArgs.java
index 843abedc..399b6623 100644
--- a/server/src/main/java/com/genymobile/scrcpy/daemon/DaemonArgs.java
+++ b/server/src/main/java/com/genymobile/scrcpy/daemon/DaemonArgs.java
@@ -99,14 +99,22 @@ public final class DaemonArgs {
         }

         if (optionsKv != null && !optionsKv.isEmpty()) {
-            for (String line : optionsKv.split("\n", -1)) {
-                String trimmed = line.trim();
+            int start = 0;
+            int end;
+            while (start < optionsKv.length()) {
+                end = optionsKv.indexOf('\n', start);
+                if (end == -1) {
+                    end = optionsKv.length();
+                }
+                String line = optionsKv.substring(start, end);
+                start = end + 1;
+
+                String trimmed = line.trim();
                 if (trimmed.isEmpty()) {
                     continue;
                 }
                 int eq = trimmed.indexOf('=');
                 if (eq == -1) {
-                    Ln.w("DaemonArgs.mergeOptions: ignoring malformed option line: " + trimmed);
                     continue;
                 }
                 String key = trimmed.substring(0, eq);
@@ -115,10 +123,10 @@ public final class DaemonArgs {
                     Ln.w("DaemonArgs.mergeOptions: ignoring blocked option key in CONFIGURE_SESSION: " + key);
                     continue;
                 }
                 map.put(key, value);
             }
         }

         List<String> list = new ArrayList<>(map.size() + 1);
