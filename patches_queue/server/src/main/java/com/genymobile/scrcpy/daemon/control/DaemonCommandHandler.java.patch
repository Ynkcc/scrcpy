diff --git a/server/src/main/java/com/genymobile/scrcpy/daemon/control/DaemonCommandHandler.java b/server/src/main/java/com/genymobile/scrcpy/daemon/control/DaemonCommandHandler.java
index 2da510f6..80521295 100644
--- a/server/src/main/java/com/genymobile/scrcpy/daemon/control/DaemonCommandHandler.java
+++ b/server/src/main/java/com/genymobile/scrcpy/daemon/control/DaemonCommandHandler.java
@@ -81,8 +81,8 @@ public final class DaemonCommandHandler implements ControlMessageExtension {
         }
     }

-    private final ExecutorService interactiveExecutor = Executors.newSingleThreadExecutor(new LooperThreadFactory("daemon-cmd-interactive"));
-    private final ExecutorService lifecycleExecutor = Executors.newFixedThreadPool(4, new LooperThreadFactory("daemon-cmd-lifecycle"));
+    private static final ExecutorService interactiveExecutor = Executors.newFixedThreadPool(4, new LooperThreadFactory("daemon-cmd-interactive"));
+    private static final ExecutorService lifecycleExecutor = Executors.newFixedThreadPool(16, new LooperThreadFactory("daemon-cmd-lifecycle"));
     private final Map<Integer, HandlerEntry> registryMap = new HashMap<>();

     public DaemonCommandHandler(Controller controller,
@@ -113,8 +113,7 @@ public final class DaemonCommandHandler implements ControlMessageExtension {
     }

     public void close() {
-        interactiveExecutor.shutdownNow();
-        lifecycleExecutor.shutdownNow();
+        // Shared executors are not shut down per-session.
     }

     /**
@@ -345,4 +344,11 @@ public final class DaemonCommandHandler implements ControlMessageExtension {
         }
         return true;
     }
+
+    /** Shut down the shared executors. Called from DaemonServer on shutdown. */
+    public static void shutdownSharedExecutors() {
+        interactiveExecutor.shutdownNow();
+        lifecycleExecutor.shutdownNow();
+    }
 }
+